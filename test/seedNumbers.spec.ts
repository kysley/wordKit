// import {seedNumbers} from '../src/seedNumbers';

describe('gen', () => {
  it('runs', () => {
    console.log('');
  });
  //   it('will return the same values with the same seed', () => {
  //     const gen1 = seedNumbers(250, 250, {seed: 'poop lol fun'});
  //     const gen2 = seedNumbers(250, 250, {seed: 'poop lol fun'});
  //     expect(gen1.list).toEqual(gen2.list);
  //   });
  //   it("isn't a fluke", () => {
  //     const gen1 = seedNumbers(100, 250, {seed: 'poop lol'});
  //     const gen2 = seedNumbers(100, 250, {seed: '21341235'});
  //     expect(gen1.list).not.toEqual(gen2.list);
  //   });
});
